# 2026-07-06

## 작업 내용

- GitBrief-be 초기 스캐폴딩 (NestJS + TypeScript, `@nestjs/cli`)
- 모듈 뼈대 생성: `auth`, `repositories`, `summaries`, `reviews`, `common`
  - `common`: Supabase 클라이언트 프로바이더 (`SUPABASE_CLIENT`)
  - `summaries`: Gemini 클라이언트 프로바이더 (`GEMINI_CLIENT`), `summarizeDiff` 뼈대
  - `repositories`: GitHub 레포/브랜치 diff 조회 메서드 뼈대
  - `auth`: GitHub OAuth 콜백 라우트 뼈대 (`GET /auth/github/callback`)
  - `reviews`: 커밋 컨벤션 검사 뼈대 (`POST /reviews/commit-message`)
- `@nestjs/config`, `@google/generative-ai`, `@supabase/supabase-js` 의존성 추가
- `.env.example` 추가 (Supabase, GitHub OAuth, Gemini 키 placeholder)
- `.gitignore` 정비 (`/dist`, `/node_modules` 등 빠져 있던 항목 추가)
- `npm run build` 통과 확인 후 초기 커밋/푸시 (`main` 브랜치)

## 다음 작업

- GitHub OAuth code → access token 교환 실제 구현
- GitHub REST API 연동 (레포 목록, 브랜치 diff)
- Gemini 프롬프트 상세 설계 및 3줄 요약 파싱 로직
- Supabase 스키마 설계 (유저/레포지토리/브랜치/요약본)
